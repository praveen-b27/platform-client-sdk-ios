import 

API.basePath = ""
API.accessToken = "cwRto9ScT..."

let pageSize: Int = 25 // Page size
let pageNumber: Int = 1 // Page number
let access: [String] = [GetContentmanagementWorkspacesRequest.Access_getContentmanagementWorkspaces.enummember.rawValue] // Requested access level.
let expand: [String] = [GetContentmanagementWorkspacesRequest.Expand_getContentmanagementWorkspaces.enummember.rawValue] // Which fields, if any, to expand.

// Code example
GetContentmanagementWorkspacesRequest.getContentmanagementWorkspaces(pageSize: pageSize, pageNumber: pageNumber, access: access, expand: expand) { (response, error) in
    if let error = error {
        dump(error)
    } else if let response = response {
        print("GetContentmanagementWorkspacesRequest.getContentmanagementWorkspaces was successful")
        dump(response)
    }
}