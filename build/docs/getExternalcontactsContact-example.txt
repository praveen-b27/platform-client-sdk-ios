import 

API.basePath = ""
API.accessToken = "cwRto9ScT..."

let contactId: String = "" // ExternalContact ID
let expand: [String] = [GetExternalcontactsContactRequest.Expand_getExternalcontactsContact.enummember.rawValue] // which fields, if any, to expand (externalOrganization,externalDataSources)

// Code example
GetExternalcontactsContactRequest.getExternalcontactsContact(contactId: contactId, expand: expand) { (response, error) in
    if let error = error {
        dump(error)
    } else if let response = response {
        print("GetExternalcontactsContactRequest.getExternalcontactsContact was successful")
        dump(response)
    }
}