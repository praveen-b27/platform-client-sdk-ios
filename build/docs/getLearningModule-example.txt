import 

API.basePath = ""
API.accessToken = "cwRto9ScT..."

let moduleId: String = "" // The ID of the learning module
let expand: [String] = [GetLearningModuleRequest.Expand_getLearningModule.enummember.rawValue] // Fields to expand in response(case insensitive)

// Code example
GetLearningModuleRequest.getLearningModule(moduleId: moduleId, expand: expand) { (response, error) in
    if let error = error {
        dump(error)
    } else if let response = response {
        print("GetLearningModuleRequest.getLearningModule was successful")
        dump(response)
    }
}