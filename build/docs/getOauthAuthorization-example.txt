import 

API.basePath = ""
API.accessToken = "cwRto9ScT..."

let clientId: String = "" // The ID of client

// Code example
GetOauthAuthorizationRequest.getOauthAuthorization(clientId: clientId) { (response, error) in
    if let error = error {
        dump(error)
    } else if let response = response {
        print("GetOauthAuthorizationRequest.getOauthAuthorization was successful")
        dump(response)
    }
}