import 

API.basePath = ""
API.accessToken = "cwRto9ScT..."

let filter: String = "" // Filtered results are invalid and return 403 Unauthorized.

// Code example
GetScimSchemasRequest.getScimSchemas(filter: filter) { (response, error) in
    if let error = error {
        dump(error)
    } else if let response = response {
        print("GetScimSchemasRequest.getScimSchemas was successful")
        dump(response)
    }
}