import 

API.basePath = ""
API.accessToken = "cwRto9ScT..."

let q64: String = "" // q64
let expand: [String] = [GetSearchRequest.Expand_getSearch.enummember.rawValue] // Which fields, if any, to expand
let profile: Bool = true // profile

// Code example
GetSearchRequest.getSearch(q64: q64, expand: expand, profile: profile) { (response, error) in
    if let error = error {
        dump(error)
    } else if let response = response {
        print("GetSearchRequest.getSearch was successful")
        dump(response)
    }
}