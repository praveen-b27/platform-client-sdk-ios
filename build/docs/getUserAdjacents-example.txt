import 

API.basePath = ""
API.accessToken = "cwRto9ScT..."

let userId: String = "" // User ID
let expand: [String] = [GetUserAdjacentsRequest.Expand_getUserAdjacents.enummember.rawValue] // Which fields, if any, to expand

// Code example
GetUserAdjacentsRequest.getUserAdjacents(userId: userId, expand: expand) { (response, error) in
    if let error = error {
        dump(error)
    } else if let response = response {
        print("GetUserAdjacentsRequest.getUserAdjacents was successful")
        dump(response)
    }
}