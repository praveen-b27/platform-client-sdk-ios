import 

API.basePath = ""
API.accessToken = "cwRto9ScT..."

let userId: String = "" // user Id

// Code example
GetUserPresencesMicrosoftteamsRequest.getUserPresencesMicrosoftteams(userId: userId) { (response, error) in
    if let error = error {
        dump(error)
    } else if let response = response {
        print("GetUserPresencesMicrosoftteamsRequest.getUserPresencesMicrosoftteams was successful")
        dump(response)
    }
}