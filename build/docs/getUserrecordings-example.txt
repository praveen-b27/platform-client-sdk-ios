import 

API.basePath = ""
API.accessToken = "cwRto9ScT..."

let pageSize: Int = 25 // Page size
let pageNumber: Int = 1 // Page number
let expand: [String] = [GetUserrecordingsRequest.Expand_getUserrecordings.enummember.rawValue] // Which fields, if any, to expand.

// Code example
GetUserrecordingsRequest.getUserrecordings(pageSize: pageSize, pageNumber: pageNumber, expand: expand) { (response, error) in
    if let error = error {
        dump(error)
    } else if let response = response {
        print("GetUserrecordingsRequest.getUserrecordings was successful")
        dump(response)
    }
}