import 

API.basePath = ""
API.accessToken = "cwRto9ScT..."

let performanceProfileId: String = "" // Performance Profile Id
let body: PerformanceProfile = new PerformanceProfile(...) // performanceProfile

// Code example
PutGamificationProfileRequest.putGamificationProfile(performanceProfileId: performanceProfileId, body: body) { (response, error) in
    if let error = error {
        dump(error)
    } else if let response = response {
        print("PutGamificationProfileRequest.putGamificationProfile was successful")
        dump(response)
    }
}